import React from 'react';
import {
  Platform,
  StyleSheet,
  Text,
  View,
  Button
} from 'react-native';

const Loading = (props) => {
  return (
    <View>
      <Text>Loading</Text>
    </View>
  )
}

export default Loading;